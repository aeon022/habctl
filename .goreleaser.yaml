version: 2

project_name: habctl

before:
  hooks:
    - go mod tidy

builds:
  - main: .
    binary: habctl
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
    goarch:
      - arm64
      - amd64
    ldflags:
      - -s -w -X github.com/aeon022/habctl/cmd.Version={{ .Version }}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^test:"

# Publishing to the Homebrew tap requires the HOMEBREW_TAP_TOKEN secret
# (fine-grained PAT with contents:write on aeon022/homebrew-tap).
brews:
  - repository:
      owner: aeon022
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/aeon022/habctl
    description: "Habit tracker for the terminal - streaks, AI coaching, MCP tools"
    install: |
      bin.install "habctl"
    test: |
      system "#{bin}/habctl", "version"
